server:
  port: 8101

spring:
  application:
    name: ribbon-consumer

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    # 缓存清单每隔30秒跟新一次
    registry-fetch-interval-seconds: 30